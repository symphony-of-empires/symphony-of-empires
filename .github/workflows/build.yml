name: Build

on:
  push:
  pull_request:
  release:

jobs:
  build_linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - name: "Build"
      run: |
        echo "Test"

#    - name: "Build"
#      run: |
#       cmake -DUNIT_TEST=1 -DCMAKE_BUILD_TYPE=Release -DE3D_BACKEND_OPENGL=1 .
#       make -j$(nproc)
#       make -j
#    - name: "Run"
#      run: |
#       echo "Yay!"
#       ./SymphonyOfEmpires
