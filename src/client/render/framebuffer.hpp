#pragma once

#include <GL/glew.h>

namespace UnifiedRender {
    namespace OpenGl {
        // A square on the z=0 plane
        class Framebuffer {
        public:
            Framebuffer();
        };
    }
}